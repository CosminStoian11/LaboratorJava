spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/libonline
    username: localdb
    password: Test1234
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect